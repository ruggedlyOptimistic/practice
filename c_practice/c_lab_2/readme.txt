C Lab 2 — Rectangle Calculator v2 (Structs + Pointers + Validation)
Objectives

1. Use multiple C files with a clean header API

2. Represent a rectangle with a struct

3. Use pointers to pass/return data safely in C

4. Add input validation (reject negative / non-numeric input)

5. Compile in stages and inspect artifacts (.o, symbols, sections)

6. Link and execute the binary

7. Generate a binary image (.bin) and inspect it